Ahh, is it synchronous? Wait a second, I think not...!

Assignments for the second lecture of JavaScript 2, Xamk

To run the development server and react host run:
### `npm start`

To run the tests run:
### `npm test`